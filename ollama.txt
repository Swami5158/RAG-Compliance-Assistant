Ollama : It is a tool which helps us download , run and manage LLMs(open-source) on local machines

-> When we download it locally and run it , it gets loaded into RAM and starts a sever at port 
    localhost:11434

-> We can then send request to this server and get a response , though for doing this we can use
    python libraries and CLI as well but the work happens through REST API calls only

        